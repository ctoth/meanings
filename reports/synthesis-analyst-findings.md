# Synthesis — analyst findings

**Date:** 2026-05-12
**Type:** analyst pass (phase 3 of `reports/synthesis-workflow-spec.md`) on `reports/synthesis.md`
**Method:** read `reports/synthesis.md`, the six `reports/synthesis-facet-*.md`, `reports/synthesis-inventory.md`, and spot-checked against `reports/{swanson-synthesis,argumentation-bridge-oewn,argumentation-dispatch-oewn,ranking-valuation-oewn,sense-ingestion-rebuild,psycholinguistic-regression-findings,self-loop-fix-impact,verify-argumentation-perf-fixes,gunray-grounding-demo}.md`, `reports/oewn-sense-ingestion-summary.json`, `pyproject.toml`. `uv run pytest`: **60 passed in 0.62s**.

Flags only. No fixes applied.

---

## Findings

### 1. The §1↔§2 stable-extension hole is acknowledged but the synthesis still uses the contradicted framing in §0/§5/§8. — should-fix

(a) **Issue.** §2 of `reports/synthesis.md` says "**Many MinSets, none privileged** = stable-extension multiplicity (a 2-cycle `A↔B` has exactly two stable extensions)" — and then, two bullets later, "**The Kernel AF has no stable extension** … So 'MinSet ↔ stable-extension outsiders' does *not* hold here; there is no stable extension to be the outsider set of." The synthesis does flag this ("does not hold here") and the math facet's gloss ("you must ground something") is quoted — but the *headline* equivalence "MinSet = stable-extension multiplicity" is restated without qualification in §0 (implicitly via "MinSet … minimal enforcement set"), in §5 row "Argumentation glue" ("showing MinSet = stable extension"), in §6 item 3 ("stable extensions = candidate groundings, `enforce_skeptical` = the MinSet(s)"), and in §8 ("MinSet = stable extension"). A reader who stops at any of those sections gets a claim §2 itself refutes for OEWN.
(b) **Evidence.** `reports/synthesis.md` §2 (bullet 2 vs bullet 4 of "the scaling result"); §5 table row "Argumentation glue"; §6 item 3; §8 bullet "Philosophy". Contradicted by `reports/argumentation-bridge-oewn.md` "Stable-extension probe" and `reports/argumentation-dispatch-oewn.md` TL;DR ("the Kernel has 0 stable extensions … the structural MinSet is empty").
(c) Severity: **should-fix** (the synthesis contains the correction, but inconsistently — a critic will pick the unqualified instance).
(d) Minimal fix: everywhere the "MinSet = stable extension" identity appears, append "(holds per-2-cycle / per-SAT-SCC; the *whole* OEWN Kernel AF is UNSAT for stable, so this identity does not lift to the Kernel — `reports/argumentation-bridge-oewn.md`)". One clause, four places.

### 2. The §0 "Up-Goer / honestly" framing over-claims relative to §4. — should-fix

(a) **Issue.** §0 calls the deliverable "the words you'd need to bootstrap English, done honestly" with no hedge. §4 explicitly says the project "should not claim … that a MinSet is a set of ultimate semantic primitives … that a raw graph node is already a human word." §0 is the abstract; it states as settled exactly what §4 forbids claiming. Also §0 says the kernel-Core-Satellites-MinSet decomposition is "bounded above by Harnad's symbol-grounding limit" — Harnad is a *constraint that prevents over-claiming*, not an upper bound the decomposition computes against; the phrasing reads as if the FVS size is a quantitative bound on the grounding set, which it isn't (the grounding set *is* an FVS; Harnad says even that isn't semantically sufficient).
(b) **Evidence.** `reports/synthesis.md` §0 vs §4 ("Should not claim"). The "bounded above by Harnad" phrasing also appears in `reports/synthesis-inventory.md` §0, so it is inherited, not introduced — but the synthesis is the place to fix it.
(c) Severity: **should-fix**.
(d) Minimal fix: §0 — "the words you'd need to bootstrap English, *as far as the definitional wiring can tell you* (not a claim about human semantic primitives — see §4)"; drop "bounded above by Harnad's symbol-grounding limit" or rephrase to "with Harnad's symbol-grounding problem as the standing reason the graph result is not itself a semantics."

### 3. "argumentation grounded at 160k in ~1.5 s … verified MERGE" — the verified timing was measured against the *local working tree*, not the pinned commit. — should-fix

(a) **Issue.** §2 and §6 of `reports/synthesis.md` and the engineering facet both present "grounded at 160k in ~1.5 s, verified MERGE (`reports/verify-argumentation-perf-fixes.md`: 806 tests pass …)" as a property of the currently pinned `argumentation` commit `4a1c31cc`. But `reports/verify-argumentation-perf-fixes.md` Check 4 says explicitly the timing run imported `argumentation.dung` "from the local `C:\Users\Q\code\argumentation\src` working tree via a verification-only `sys.path.insert` (the meanings venv pins `formal-argumentation` to old commit `8d28624`)". The verifier ran the *uncommitted working tree*; it did not verify that what got pushed and pinned as `4a1c31cc` is byte-identical to what it timed. The 806-test count is from the same working tree. This is exactly the "is that re-confirmed against the *currently pinned* commit" gap the analyst brief names. (`pyproject.toml` does now pin `rev = "4a1c31cc45a2a4a67b45a72941812e1098ba7018"` — so the *pin* is current; what's unverified is that `4a1c31cc` == the timed/tested tree.)
(b) **Evidence.** `reports/verify-argumentation-perf-fixes.md` Check 4 ("imported … from the local … working tree"; "the meanings venv pins formal-argumentation to old commit 8d28624"); `reports/synthesis.md` §2 bullet 1, §6 item 1; `reports/synthesis-facet-engineering.md` §2 ("verified MERGE"). Note `reports/argumentation-dispatch-oewn.md` *did* run `grounded` (4.33 s full graph, 0.44 s Kernel) through the actual installed dependency — but at a different timing (4.33 s, not 1.5 s) and it doesn't state which commit the installed dep was at when that ran.
(c) Severity: **should-fix** (the fix landed; the chain of custody from "verified" to "pinned" has a one-link gap; cheap to close).
(d) Minimal fix: add to §6 item 1 / §7 "`formal-argumentation` is pinned" bullet: "the 806-tests-pass / ~1.5 s figures were measured on the local `argumentation` working tree pre-push; a re-run of `uv run pytest` against the installed pinned `4a1c31cc` would close the chain (the dispatcher run that imports the installed dep clocked `grounded(full)` at 4.33 s, not 1.5 s — `reports/argumentation-dispatch-oewn.md`)."

### 4. The synthesis quotes two different timings for the same `grounded` call (~1.5 s vs 4.33 s) without reconciling them. — should-fix

(a) **Issue.** §2 bullet 1: "computes the grounded extension over the full 160,010-node OEWN attack-AF in **~1.5 s**." §2 bullet 4 (the dispatcher): says the dispatcher's `grounded` "delegates to the now-linear upstream" — and `reports/argumentation-dispatch-oewn.md` reports that call at **4.33 s** for the full graph. The synthesis presents both numbers (1.5 s in the bridge bullet, the dispatcher bullet doesn't restate the seconds but the cited report has 4.33 s) with no note that they're the same operation on the same graph with a ~3× spread. Likely cause: different host / different load / `verify-argumentation-perf-fixes` median-of-6 vs a single dispatcher run — but the synthesis should say so rather than leave a reader to discover it in the JSON.
(b) **Evidence.** `reports/synthesis.md` §2 bullets 1 and 4; `reports/verify-argumentation-perf-fixes.md` ("~1.3–1.7 s … median ~1.5 s"); `reports/argumentation-dispatch-oewn.md` ("|grounded(full 160010-node graph)| = 5043 in 4.33 s").
(c) Severity: **should-fix**.
(d) Minimal fix: §2 — "~1.5 s (verifier, median of 6, local working tree) / 4.33 s in the dispatcher run (`reports/argumentation-dispatch-oewn.md`) — same linear algorithm, the spread is host/load, both vastly under the pre-fix >200 s."

### 5. The synthesis nowhere flags that the Swanson scoreboard's `#3` controllability Jaccard numbers in §1 are inconsistent with `reports/swanson-synthesis.md` and `reports/maximum-matching-oewn.json`. — should-fix

(a) **Issue.** `reports/synthesis.md` §1 bullet `#3` says: "full digraph 118,744 drivers (74%), Jaccard with the FVS seed ≈ 0.02; Kernel subgraph drivers comparable in *size* to the seed but Jaccard ≈ 0.04". `reports/swanson-synthesis.md` "Controllability" says: "Full digraph: 118,744 driver nodes (74.2%); overlap with the 5,044-node FVS-seed = 2,207, Jaccard **0.018**. Kernel subgraph: 3,916 driver nodes (21.6%) … only **316 nodes in common** (Jaccard **0.037**)." `reports/synthesis-inventory.md` §1.4 quotes a *third* set ("full digraph 118,744 drivers (74.2%), Jaccard with the 2,370-node FVS seed = 0.012; Kernel subgraph 2,785 drivers (21.7%), … Jaccard 0.052; 266/288 Core nodes are drivers") — those are the *pre-self-loop-fix* numbers (2,370-node seed, 288-node Core). So three reports carry three Jaccards for the "FVS seed vs drivers" comparison and the synthesis's "≈ 0.02 / ≈ 0.04" matches the *post-fix* `swanson-synthesis.md` rounding but the synthesis doesn't say which it is or that the inventory's are stale. The synthesis §1 `#3` also reuses the inventory's stale framing "~1.5% FVS seed vs ~74% matching drivers" — 5,044/160,010 ≈ 3.15%, not 1.5%; 1.5% was the *pre-fix* 2,370/160,010. (See finding 6.)
(b) **Evidence.** `reports/synthesis.md` §1 `#3`; `reports/swanson-synthesis.md` "Controllability: a clean divergence"; `reports/synthesis-inventory.md` §1.4 (controllability); `reports/maximum-matching-oewn.json` (not re-opened — flagged by both reports as the source).
(c) Severity: **should-fix** (the qualitative point — "a divergence, not an identity" — is unaffected; the specific Jaccards are inconsistent across the corpus and the synthesis doesn't disambiguate).
(d) Minimal fix: §1 `#3` — pin to the post-fix `swanson-synthesis.md` numbers explicitly (5,044-node seed, Jaccard 0.018 full / 0.037 Kernel, 3,916 Kernel drivers) and replace "~1.5%" with "~3.15%".

### 6. STALE NUMBER: "~1.5% / 8% kernel" / "the OEWN definition graph (~1.5% FVS seed vs ~74% matching drivers)" in §1 is a pre-self-loop-fix figure quoted as current. — should-fix

(a) **Issue.** `reports/synthesis.md` §1 `#3` (the controllability bullet) twice cites "~1.5% FVS seed" (once as "the OEWN definition graph (~1.5% FVS seed vs ~74% matching drivers) squarely in the biological-network regime"). 1.5% = 2,370/160,010 = the **pre-self-loop-fix** combinatorial seed. Post-fix it's 5,044/160,010 ≈ 3.15% (`exact-small-greedy`) or 3,620/160,010 ≈ 2.26% (`bounded-scc`). The synthesis elsewhere (§3, §5, §8, §6) consistently uses the post-fix 18,151 / 510 / 5,044 numbers and even has an "honest caveat" about stale numbers — so this one slipped through. (The biology-regime comparison still holds at 3.15% — Zañudo's gene-reg range is 1–18% FVS — so the *conclusion* survives; the *number* is stale.)
(b) **Evidence.** `reports/synthesis.md` §1 `#3` (two occurrences of "~1.5%"); `reports/self-loop-fix-impact.md` table (post-fix seed 5,044 `exact-small-greedy` / 3,620 `bounded-scc`; pre-fix 1,824 / 733 — note: even the *pre-fix* `exact-small-greedy` seed was 1,824, not 2,370; 2,370 is the pre-fix number quoted in `swanson-synthesis.md`/`spectral-valuation-oewn.md` and `psycholinguistic-regression-findings.md`, which used a different solver run); `reports/psycholinguistic-regression-findings.md` §0 (Kernel 12,853 / seed 2,370 — explicitly pre-fix).
(c) Severity: **should-fix**.
(d) Minimal fix: §1 `#3` — "~3.15% FVS seed (post-self-loop-fix, 5,044/160,010) vs ~74% matching drivers — still inside Zañudo's 1–18% gene-regulatory FVS regime."

### 7. Uncited / unsupported: §0's lineage `reports/synthesis-minimal-core-to-expansion.md` and §1's `reports/stale-numbers-audit.md` — cited files that may not exist / weren't in the inputs. — should-fix

(a) **Issue.** `reports/synthesis.md` §0 ends "(`reports/synthesis-minimal-core-to-expansion.md`)" and §1 cites "(numbers post the self-loop fix per `reports/stale-numbers-audit.md`)". Neither was in the analyst's input set; `reports/synthesis-inventory.md` §1.7 cites `reports/synthesis-minimal-core-to-expansion.md` too, and `reports/swanson-synthesis.md` recommended-next item 3 cites `reports/stale-numbers-audit.md` — so the synthesis is *inheriting* these citations, not inventing them, but the analyst could not confirm the files exist or that they say what's attributed. (The lineage `Masterman 1961 → Harnad 1990 → …` is the kind of claim that needs the cited file to actually contain it.) Flagging for the verifier to confirm-or-correct rather than asserting they're missing.
(b) **Evidence.** `reports/synthesis.md` §0 final paren; §1 first paren. Cross-ref `reports/synthesis-inventory.md` §1.7; `reports/swanson-synthesis.md` "Recommended next work" item 3.
(c) Severity: **should-fix** (verify the two filenames resolve; if `reports/synthesis-minimal-core-to-expansion.md` doesn't exist the lineage paragraph is uncited).
(d) Minimal fix: `ls reports/synthesis-minimal-core-to-expansion.md reports/stale-numbers-audit.md`; if absent, either add the files or cite the file that actually carries the lineage / audit.

### 8. LOAD-BEARING-BUT-UNVERIFIED: the sense-rebuild's "zero surviving gloss self-loops" and "4,562 short non-lexical artifacts" rest entirely on the lexicality classifier + the rebuild script; no independent check exists, and the synthesis's §7 says so but §3 and §4 state them as established. — should-fix

(a) **Issue.** §3 of `reports/synthesis.md`: "**Zero surviving gloss self-loops** … the self-loop-artifact prediction *passes*"; "**4,562 short non-lexical artifacts** … quarantined or excluded". §4 ("Can defend"): "the sense-level rebuild dissolves the gloss-self-loop artifacts" — listed under things the project *can defend*. §7 then says "The sense-level rebuild's numbers might be cooked. 'Zero surviving gloss self-loops' is a strong claim; '4,562 short non-lexical artifacts quarantined' depends on the lexicality classifier's precision, which hasn't been independently audited". So the synthesis simultaneously puts "dissolves the artifacts" in the defensible column (§4) and in the unverified column (§7). The only evidence is `reports/sense-ingestion-rebuild.md` + `reports/oewn-sense-ingestion-summary.json`, both products of the same un-audited `scripts/sense_ingestion_rebuild.py` / `src/meanings/lexicality.py` / `src/meanings/identity_clusters.py` run; agenda item #7 ("a verifier pass on the new src … no independent audit yet") confirms there is no check. Also: the summary JSON's `short_artifact_counts` sum to 152+2+4396+10+2 = **4,562** (matches), but `lexicality_counts` has no `lexical-word` entry in the *short-artifact* breakdown — meaning the classifier never tags a short token `lexical-word`? That's the `no`/`No`/`s`/`e`/`g`/`ph`/`th`/`ax`/`axe` short-token whitelist the upgoer note demanded — if zero short tokens come out `lexical-word`, the whitelist either isn't wired or admits nothing, which would be a real gap the synthesis doesn't mention. (Could not verify from the report alone; flag for the verifier.)
(b) **Evidence.** `reports/synthesis.md` §3 (¶ "The rebuild ran"), §4 (¶ "Can defend"), §7 (¶ "The sense-level rebuild's numbers might be cooked"), §6 item 7; `reports/sense-ingestion-rebuild.md`; `reports/oewn-sense-ingestion-summary.json` (`short_artifact_counts`, `lexicality_counts`).
(c) Severity: **should-fix** (the §7 caveat exists; §4 should not list "dissolves the artifacts" as defensible without the same hedge, and the short-token-whitelist question should be checked).
(d) Minimal fix: §4 — move "the sense-level rebuild dissolves the gloss-self-loop artifacts" from "Can defend" to a "claimed, pending the §7 verifier pass" line, or annotate it "(self-reported by the rebuild run; not yet independently audited — §7, agenda #7)". Separately, verify whether any short token is tagged `lexical-word` in the rebuild output.

### 9. LOAD-BEARING-BUT-UNVERIFIED: the dispatcher's `stable_exists` greedy-sweep — the synthesis presents "structural MinSet count = 0" / "whole Kernel UNSAT" as a fresh independent confirmation when the dispatcher is largely a re-run of the same z3 calls the bridge made. — nitpick

(a) **Issue.** §2 of `reports/synthesis.md`: the dispatcher "re-derives the bridge's findings (whole Kernel UNSAT, structural MinSet count = 0) in ~12 s … Caveats: … `stable_exists` is a greedy topological sweep — definitive for the OEWN Kernel, could over-report UNSAT in pathological context-dependent cases (the exact-count path uses full DAG DP without that issue)." This is faithfully copied from `reports/argumentation-dispatch-oewn.md` "Caveats". Fine. The mild over-claim: the dispatcher's "agreement with the bridge" table (`reports/argumentation-dispatch-oewn.md`) shows it agrees because it *makes the same z3 call on the same 8,138-node SCC* (the giant SCC is UNSAT either way); the 41-iso-class cache and the greedy sweep are new *machinery* but the UNSAT verdict is not an *independent* re-derivation — it's the same oracle. The synthesis says "re-derives … in ~12 s, dominated by the one z3 call on the giant core" which is accurate, but the framing in §6 item 1 ("✓ `argumentation_dispatch` (SCC+z3+FVS-backdoor, 99.6 % isomorphism-cache hit)") and §7's "the *demonstrator* … hasn't been built" could mislead a reader into thinking the dispatcher independently corroborated UNSAT. It re-confirmed via the same z3.
(b) **Evidence.** `reports/argumentation-dispatch-oewn.md` "Agreement with argumentation-bridge-oewn.md" table + "Caveats"; `reports/synthesis.md` §2 (dispatcher bullet).
(c) Severity: **nitpick**.
(d) Minimal fix: §2 — "re-derives the bridge's findings via the *same* per-SCC z3 oracle but with the SCC-condensation front-end + iso-cache the bridge lacked (so it's a faster re-run, not an independent solver)".

### 10. The `gunray` demo's OEWN slice was taken from a *different graph* (`build_lemma_graph`, 151,622 nodes / 25,185-node Kernel) than the canonical `paper-wordnet` (160,010 / 18,151) — §2 of the synthesis calls it "a real ~9-word strongly-connected slice of the OEWN Kernel" without that distinction. — nitpick

(a) **Issue.** `reports/gunray-grounding-demo.md` Part 3: "the **smallest cyclic strongly-connected component** of the OEWN:2024 *lemma-level* Kernel (25,185 nodes; the … 151,622-node definition graph)". That's `build_lemma_graph` (one node per `lemma::pos`-collapsed-to-lemma, all POS merged), not `build_paper_wordnet_graph` (160,010 nodes / 18,151-node Kernel — the "measuring stick"). `reports/synthesis.md` §2 says "Demonstrated on a real ~9-word strongly-connected slice of the OEWN Kernel" — true, but "the OEWN Kernel" in every other section of the synthesis means the 18,151-node paper-wordnet Kernel; here it silently means a different 25,185-node object. A reader can't tell. Also the demo's `no`/Nobelium polysemy case is a *hand-built `gunray` theory* with hardcoded rules (`r_neg`, `r_nob`, `r_check`), not derived from any graph — §2's "(a gloss-type-check argument defeats the Nobelium reading of `no`)" reads as if it fell out of the data; it's an illustrative toy. (Both are fine *as demos*; the synthesis should not blur "demonstrator on a hand-picked slice / hand-built theory" with "result on the OEWN graph".)
(b) **Evidence.** `reports/gunray-grounding-demo.md` Part 3 (first ¶) and Part 4; `reports/synthesis.md` §2 (gunray bullet).
(c) Severity: **nitpick**.
(d) Minimal fix: §2 — "a real ~9-word SCC of the OEWN *lemma-level* Kernel (the `build_lemma_graph` graph, not the `paper-wordnet` one)" and "a hand-built `gunray` theory illustrating the polysemy case (`no` negation vs Nobelium)".

### 11. Internal inconsistency: §3 says the strict-seed export "reflects whatever `scripts/sense_ingestion_rebuild.py` does (verify which)" but the same paragraph already states a definite number — 834-IC strict seed — as if settled. — nitpick

(a) **Issue.** §3 of `reports/synthesis.md`: "Exports: `data/oewn-sense-strict-seed.json` (834-IC strict lexical seed) …" — a definite figure — and then in the "honest caveat" paragraph: "the datamodel-Claude facet flags the one design decision the twin drafts didn't fully pin down: whether the strict-seed Kernel should be computed on the **IC-projected** graph (Claude's choice) or on the **raw sense graph with IC-restriction applied only at seed-export** … the two give different Kernel numbers, the upgoer note doesn't decide it, and the current `data/oewn-sense-strict-seed.json` reflects whatever `scripts/sense_ingestion_rebuild.py` does (verify which)." So the 834 number is presented as a fact in one sentence and as "we don't know which method produced it" two sentences later. (`reports/oewn-sense-ingestion-summary.json` has `strict_seed_ic_count: 834` and `strict_seed_sense_count: 860` — so the script did *something* definite; what's unverified is whether that something is the IC-projected-Kernel or the sense-Kernel-then-restrict path. The datamodel-claude facet §8 is the source of the open question.)
(b) **Evidence.** `reports/synthesis.md` §3 (¶ "The rebuild ran" gives 834; ¶ "The honest caveat" says "verify which"); `reports/synthesis-facet-datamodel-claude.md` §8 (the open question); `reports/oewn-sense-ingestion-summary.json` (`strict_seed_ic_count`).
(c) Severity: **nitpick**.
(d) Minimal fix: §3 — "834-IC strict lexical seed *as produced by the current script's path (which of the two §5-agenda definitions it implements is unverified — `reports/synthesis-facet-datamodel-claude.md` §8)*."

### 12. §6 item 7 / §3 say "11 + 25 new tests pass"; the actual count is 60 total and the breakdown isn't sourced. — nitpick

(a) **Issue.** `reports/synthesis.md` §6 item 7: "(11 + 25 new tests pass, but no independent audit yet)". §3: "25 new tests" (dispatcher) and "the rebuild ran … `src/meanings/lexicality.py` … `scripts/sense_ingestion_rebuild.py`" with no test count for the sense-rebuild modules. `uv run pytest` → **60 passed**. Inventory said the suite was ~24 after the bridge round; +25 dispatcher = 49; +11 = 60 — so "11 + 25" reconciles to 60 if the 11 are the sense-rebuild/lexicality/identity-cluster tests. But no report in the input set states "11 new tests for lexicality/identity_clusters/sense-builder" — `reports/sense-ingestion-rebuild.md` has no test count at all; `reports/argumentation-dispatch-oewn.md` says "25 new tests" only in the synthesis, not in the dispatch report itself (the dispatch report doesn't give a test count). So the "11 + 25" breakdown is asserted in the synthesis without a citable source.
(b) **Evidence.** `reports/synthesis.md` §3, §6 item 7; `reports/sense-ingestion-rebuild.md` (no test count); `reports/argumentation-dispatch-oewn.md` (no test count); `uv run pytest` → 60 passed.
(c) Severity: **nitpick**.
(d) Minimal fix: §6 item 7 — "(`uv run pytest` → 60 passed: ~24 prior + the dispatcher tests + the lexicality/IC/sense-builder tests; no independent audit yet)" — or cite the commit that added them.

### 13. §2 "806 tests pass, 257 Dung + 120 bipolar instances bit-exact … 50k-node graph in 0.13 s" — the synthesis presents the verifier's numbers correctly but omits the verifier's own load-bearing caveat that the OEWN 160k case is ~1.5 s "not sub-second", traceable to a pre-existing module convention. — nitpick

(a) **Issue.** `reports/synthesis.md` §2 bullet 1 quotes "50k-node graph in 0.13 s" and "~1.5 s" for OEWN but doesn't carry forward `verify-argumentation-perf-fixes.md`'s explicit note: "the binding acceptance bar — `grounded_extension` on a 50k+-node sparse graph: sub-second — is met handily (0.13s at 50k). On the actual 160k/678k OEWN graph it is ~1.5s, i.e. **not 'well under a second'** … The residual constant factor is inherent to the module's existing `dict[str, frozenset[str]]` index convention." §6 item 8 ("Further `../argumentation` perf — int-indexed adjacency … currently ~1.5 s, constant-factor `frozenset`-rebuild overhead") *does* carry it — so the synthesis has the caveat, just not next to the claim. Minor.
(b) **Evidence.** `reports/synthesis.md` §2 bullet 1, §6 item 8; `reports/verify-argumentation-perf-fixes.md` Check 4 + "Why MERGE despite the 1.5s".
(c) Severity: **nitpick**.
(d) Minimal fix: none strictly needed; optionally cross-reference §6 item 8 from §2 bullet 1.

### 14. The ranking-valuation report's headline "+0.023" matches the synthesis's "+0.023", but the synthesis drops the report's caveat that the full-graph pass is iteration-capped and (per the report's own §3) the full-graph table is a placeholder. — nitpick

(a) **Issue.** `reports/synthesis.md` §1 ("The empirical adjudication") and §1 closing ¶: "best incremental R² over `log(out-degree)` ≈ +0.023 on a ~0 baseline". `reports/ranking-valuation-oewn.md` §0/§2a: the +0.0229 headline is `kernel/burden/concreteness` on a baseline R² of 0.0001, and the report's §3 (full-graph numbers) is an explicit placeholder ("FULL-GRAPH RESULTS — filled in when the full-graph pass completes") with the prose saying "see the JSON". So the +0.023 is a *kernel-subgraph, converged* number; the synthesis presents it without that scope tag, and the underlying full-graph result is, per the report, not actually written into the report (only the JSON). Verdict ("collapses to degree, wrong sign") is robust either way — but the synthesis is quoting a number whose source report flags its own incompleteness.
(b) **Evidence.** `reports/synthesis.md` §1 (¶ "The ranking-semantics follow-up"); `reports/ranking-valuation-oewn.md` §0, §2a, §3 (placeholder block).
(c) Severity: **nitpick**.
(d) Minimal fix: §1 — "+0.023 (kernel-subgraph, burden/concreteness, on a ≈0 baseline; the full-graph pass was iteration-capped and its numbers live only in `reports/ranking-valuation-oewn.json`)".

### 15. §7 "open for a critic" misses two real soft spots. — should-fix

(a) **Issue.** §7 of `reports/synthesis.md` lists six "things a critic should poke" but omits: (i) **the chain-of-custody gap on the pinned `argumentation` commit** (finding 3 — "verified" was the working tree, not `4a1c31cc`); a critic attacking "the scaling result is real" would go straight there. (ii) **the apples-to-different-fruit comparison is even worse than §3's caveat admits**: the sense-level graph (212,478 nodes) is built by `build_sense_level_paper_wordnet_graph` which *resolves gloss references to the intended sense* — i.e. it does *more* disambiguation than the lemma graph, and the resolution stats (`reports/oewn-sense-ingestion-summary.json`: `ambiguous_skipped: 499,860` of 925,283 candidate matches — **54% of all gloss-token matches are dropped as ambiguous**) mean the sense graph has far fewer *edges per node* than the lemma graph (418,094 edges / 212,478 nodes ≈ 1.97 vs 677,823 / 160,010 ≈ 4.24). A smaller Kernel on a graph where you threw away half the edges is not surprising and not obviously "the artifacts dissolved" — it could be "we dropped the edges that would have formed cycles". §7 says "the Kernel-shrinkage is across different node surfaces" but not "across a 2×-sparser edge set because half the gloss matches were dropped". That's the strongest version of the "numbers might be cooked" charge and it's not stated.
(b) **Evidence.** `reports/oewn-sense-ingestion-summary.json` (`candidate_matches: 925283`, `ambiguous_skipped: 499860`, `edge_count: 418094`, `node_count: 212478`); `reports/sense-ingestion-rebuild.md` "Resolution Stats"; `reports/synthesis.md` §7, §3 ("honest caveat").
(c) Severity: **should-fix** (a phase-3 critic is *assigned* "the sense-rebuild-numbers-are-cooked charge" per the spec; the synthesis should pre-empt the strongest form, which is the dropped-edges one, not just "different node surface").
(d) Minimal fix: §7 bullet "The sense-level rebuild's numbers might be cooked" — add: "and the sense graph has ~2× fewer edges per node than the lemma graph (1.97 vs 4.24) because 54% of gloss-token matches (499,860 / 925,283) were dropped as ambiguous — so the smaller Kernel partly reflects dropped edges, not only dissolved artifacts; the honest claim needs an edge-budget-controlled comparison." Add a new bullet for the `4a1c31cc` chain-of-custody gap (finding 3).

### 16. Over-claim: §4 "psycholinguistic salience is largely *mirrored* in graph structure on the current OEWN artifact" is in "Can defend" — but the regression's own §6.3 says the result is *equally* consistent with the opposite causal story; "mirrored" is the right verb only in the structure→norms direction, and the regression can't tell which way it goes. — nitpick

(a) **Issue.** `reports/synthesis.md` §4 "Can defend": "psycholinguistic salience is largely *mirrored* in graph structure on the current OEWN artifact". `reports/psycholinguistic-regression-findings.md` §6.3: "'Block 2 adds nothing over block 1' is equally consistent with (i) meaning is relational … and (ii) the relations were constructed from psycholinguistic salience … These are causally opposite stories with the same regression signature." "Mirrored" is symmetric and defensible; but the synthesis §4 then immediately (in "Should not claim") says "the structure may mirror the norms *because* the graph is a cultural artifact" — so it does carry the confound. The mild issue: a casual reader of just the "Can defend" line takes "mirrored in graph structure" as "the graph captures the salience" (a finding about the graph) when the equally-live reading is "the salience built the graph" (a finding about lexicographers). The §6 item 2 lead-paper bullet repeats "psycholinguistic salience is largely *mirrored* in graph structure" without the confound. Minor; the synthesis has the confound elsewhere.
(b) **Evidence.** `reports/synthesis.md` §4 (both lists), §6 item 2; `reports/psycholinguistic-regression-findings.md` §6.3.
(c) Severity: **nitpick**.
(d) Minimal fix: §4 "Can defend" — "psycholinguistic salience and graph structure are largely *co-extensive* on this artifact (which way the causation runs is undetermined — see 'Should not claim')".

---

## Summary

- **Blocker-severity findings: 0.** Nothing in `reports/synthesis.md` is wrong enough to block the document from proceeding to phase 4 — every load-bearing number traces to a source, the major contradictions (the stable-extension hole, the stale-numbers issue) are *acknowledged in the synthesis itself*, just inconsistently propagated.
- **Should-fix: 8** (findings 1, 2, 3, 4, 5, 6, 7, 8, 15 — nine items; treat 7 as a verify-and-then-fix).
- **Nitpicks: 7** (9, 10, 11, 12, 13, 14, 16).
- **Single most important finding: #3** — "argumentation grounded at 160k in ~1.5 s, verified MERGE" is presented as a property of the pinned commit `4a1c31cc`, but `reports/verify-argumentation-perf-fixes.md` measured the *local uncommitted working tree* via a `sys.path.insert` while the venv still pinned the *old* `8d28624`; the verifier never confirmed `4a1c31cc` == what it tested. The scaling result is the synthesis's flagship "retired in code, not just argued" claim (§2 lead), and the dispatcher run that *does* import the installed dep clocked the same call at 4.33 s, not 1.5 s. Close the chain with one `uv run pytest` against the installed pin and a note in §6 item 1 / §7.

**Report path:** `C:\Users\Q\code\meanings\reports\synthesis-analyst-findings.md`
