# notes: meanings repo setup + Swanson research prompts

## 2026-05-12

### Repo bootstrap (done)
- `meanings` was an uncommitted working dir. Grokked it: dictionary-kernel tooling — builds OEWN gloss digraph via `wn`, extracts Kernel/Core/Satellite layers + feedback-vertex-set seeds (Massé 2008 / Picard 2013 / Vincent-Lamarre 2014 lineage), `papers/` = literature notes, `reports/` = generated output + research dossier.
- Wrote `README.md`. Updated `.gitignore` (added `.venv/`, `__pycache__/`, `*.pyc`, `pyghidra_mcp_projects/` [stray Ghidra project, Q confirmed exclude], `unused-papers/`).
- Initial commit `0b57023`, 125 files. Created public repo `ctoth/meanings` (Q confirmed public), pushed. Remote = `origin` = `git@github.com:ctoth/meanings.git`. Default branch is `master` (flagged to Q; left as-is).

### Swanson research prompts (done)
Q asked to turn the cross-domain "what's defined by its neighbors" ideas into deep-research-agent prompts, one markdown each. In `reports/`:
- `research-swanson-1-money-numeraire.md` — symbol grounding ≡ gold-standard/numéraire problem; MinSets ≡ arbitrariness of numéraire (Sraffa standard commodity).
- `research-swanson-2-core-satellite-ecology.md` — Hanski core-satellite hypothesis (ecology 1982) ↔ dictionary Core/Satellite (2014); has a runnable bimodality check on existing OEWN JSON.
- `research-swanson-3-controllability-driver-nodes.md` — lexical grounding sets (FVS) ↔ structural controllability driver nodes (Liu-Slotine-Barabási 2011, max matching); runnable max-matching comparison on OEWN graphs.
- `research-swanson-4-yoneda-harnad.md` — is meaning Yoneda-complete? category-theory structuralism vs Harnad's grounding residue; adjudicate via extra-graph variance (AoA/concreteness) in layer membership.
- `research-swanson-5-perron-frobenius-valuation.md` — **the central one.** Dominant eigenvector of a non-negative relational matrix as canonical-anchor algorithm, rediscovered ~6× (Sraffa standard commodity, LSA semantic axes, PageRank, Bonacich/Hubbell/Katz/Pinski-Narin centrality, Markov stationary dist) with near-empty cross-citation. Repo payoff: spectral anchor (unique `v*`) dissolves the combinatorial MinSet arbitrariness; and the two compose — run PF on the strongly-connected Kernel, recursion on the reducible Rest. Has the headline runnable deliverable (Perron/PageRank ranking vs MinSet membership vs layers vs psycholinguistic overlays on existing OEWN data).
- Commits: `1e89bff` (prompts 1-4), `78d0ea4` (prompt 5). Both pushed.

### Execution launched (2026-05-12)
Q said "run all prompts with their own subagents, also ask codex, go hard." Dispatched 6 background streams:
- 5 `researcher` subagents, one per prompt file, each told to read the brief, do web research, run the in-repo computation the brief specifies (2/3/5 each have one), write `reports/swanson-<topic>-findings.md`. Agent IDs: ab71d377b6a89aa17 (1 money), a8856df375d1d1161 (2 ecology), a6b1f8872f063e1ad (3 controllability), a0b72c025bed3532b (4 yoneda), afddb8bdf1837f5e3 (5 perron-frobenius).
- Codex via `protocols:external-agents`: `codex exec --dangerously-bypass-approvals-and-sandbox`, background bash ID bvklzfk33, log `codex-swanson.log`, writing `reports/codex-swanson-review.md` — skeptical review of all 5 briefs + independent contribution to the 2 it judges strongest. No web, own knowledge.

### Progress
- ~16:27 transient DNS outage on Q's machine (~30s) killed researcher agents 3 & 4 and disrupted Codex. Network recovered. Re-dispatched 3 (ab395758bbc9dff5a) and 4 (a6a6bf774fcc6664d).
- Prompt 1 (money/numéraire) DONE — `reports/swanson-money-numeraire-findings.md`: 13-row correspondence table, disjointness evidence (closest near-miss = Sraffa–Wittgenstein/Ajit Sinha thread), 5 ranked transferable tools (top = Sraffa standard commodity → PF eigenvector of weighted Core as canonical-MinSet algorithm; runnable on existing pipeline), 6 falsifiers (the prompt's headline falsifier does NOT fire — Sraffa basics/non-basics IS an SCC decomposition), proposed paper "Meaning Has No Gold Standard" → Cognitive Science / Topics in CogSci. Couldn't get clean text of Sraffa 1960 or Patinkin — quoted via secondaries, flagged.
- Agents 2 (ecology, wrote scripts/bimodality_check.py — has missing-import bugs: sklearn/diptest not installed) and 5 (perron-frobenius, wrote scripts/perron_frobenius_oewn.py) still running. Re-run agent 3 wrote scripts/maximum_matching_oewn.py.
- Codex: check codex-swanson.log + reports/codex-swanson-review.md; may have died in outage — re-run if so.

### Completions log
- DONE: prompt 1 (money), prompt 4 (yoneda → verdict Resolution A + symmetry rider), Codex review (ranks: PF #1, controllability #2, ecology #3, money #4, yoneda #5; proposes lead paper "Discrete Grounding and Spectral Valuation in Dictionary Graphs"), prompt 5 (perron-frobenius).
- KEY RESULT prompt 5: naive hypothesis (Perron eigenvector = canonical soft grounding vocab) FALSIFIED on OEWN. Authority PageRank ranks definitional SINKS at top; seed hubs (small/large/body/plant) sit in bottom decile (median PR rank ~48k/160k). Un-damped Perron on 8138-node kernel SCC localises on technical micro-cliques (potassium/rubidium/...), λ*≈3.744, ~degree re-skin. DIAGNOSIS = surviving paper: FVS heuristic is an OUT-flow object → matching spectral object is REVERSE-PageRank on the kernel, not authority PageRank. Predicted (not run): reverse-PR ranks small/large/white/plant/body near top, ρ>0.6 with seed degree-score. Codex Prediction C called this. Files: reports/swanson-perron-frobenius-findings.md, reports/perron-frobenius-oewn.json, scripts/perron_frobenius_oewn.py.
- BLOCKER noted by multiple agents: repo ships NO psycholinguistic CSVs (annotation_sources: [], coverage 0). Need SUBTLEX-US / Kuperman 2012 AoA / Brysbaert 2014 concreteness → data/psycholinguistic/*.csv. Codex also flagged: top-definer lists polluted by sense-resolution/gloss-parsing artifacts (large[n]=garment size, born=Max Born) — clean before any occupancy/frequency test.
- STILL RUNNING: prompt 2 (ecology, ab... a8856df375d1d1161 + scripts/bimodality_check.py has missing-deps bug sklearn/diptest), prompt 3 re-run (ab395758bbc9dff5a, scripts/maximum_matching_oewn.py).

### More completions
- DONE prompt 3 (controllability re-run): computation ran. Full digraph → 118,744 drivers (74.2%); kernel subgraph → 2,785 drivers (21.7%, comparable in size to 2,370 FVS-seed but only 255 in common, Jaccard 0.052). FVS and driver sets genuinely diverge — confirms prediction. Driver-not-grounder = morphological derivatives + numeral/proper-noun acyclic frontier; grounder-not-driver = abstract relational vocab + proper-noun islands. Top transferable tool: FVS-control (Fiedler–Mochizuki / Zañudo–Yang–Albert) — unexploited bridge. Flagged: self-loop handling inconsistency in compute_kernel (`target != node` filter). Files: reports/swanson-controllability-findings.md, reports/maximum-matching-oewn.json, scripts/maximum_matching_oewn.py.
- Q said "kick those off" → launched 2 follow-up `claude` agents (background): a1f0cfc8414c31a55 = ingest psycholinguistic norms (SUBTLEX-US, Kuperman AoA, Brysbaert concreteness → data/psycholinguistic/, wire to CLI, verify coverage>0); a90c63e203ee554c6 = confirm reverse-PageRank diagnosis from prompt 5 + build src/meanings/spectral_analysis.py per Codex spec + reports/spectral-valuation-oewn.md.
- STILL RUNNING: prompt 2 (ecology) a8856df375d1d1161; the 2 follow-ups above.

### Prompt 2 (ecology) DONE — negative result
Disjointness genuine (canonical Swanson signature, both directions). But bimodality falsifier TRIGGERED: OEWN out-degree (Hanski occupancy analogue) is 74.3% zero + monotone heavy tail (log-series-like), no second hump; dip test "rejects" only because of zero-spike at huge n; GMM BIC keeps falling = straining to fit one skew. Sharpest extra falsifier: Core (n=288) and Satellite (n=12565) have ~identical out-degree distributions (means 43.8 vs 42.5) — the lexical Core/Satellite split is condensation topology (source vs downstream SCC), NOT common-vs-rare like Hanski's. Naming analogy shallower than it looks. Weak form survives (colonization-extinction process view of defining vocab across dictionary editions) but the strong prediction is contradicted by current data; testing needs diachronic definition text (LDOCE controlled-defining-vocab revisions = near-perfect natural experiment). Honest paper = cautionary cross-disciplinary note "Core and Satellite, Twice Over". Files: reports/swanson-core-satellite-findings.md, scripts/bimodality_check.py, scripts/bimodality_kernel.py (re-run: `uv run --with scikit-learn --with diptest python scripts/bimodality_kernel.py`).

ALL 5 RESEARCH PROMPTS + CODEX DONE. Still running: psycholinguistic ingest (a1f0cfc8414c31a55), spectral module re-run (aae8a900ddef8da59, actively writing src/meanings/spectral_analysis.py + scripts/spectral_report.py + tests/test_spectral_analysis.py — minor pyright issues, agent will resolve).

### Scoreboard (after the swarm)
- #1 Perron-Frobenius: naive form FALSIFIED on OEWN; surviving result = "The Wrong Eigenvector" — FVS heuristic is out-flow → matching object is REVERSE-PageRank on kernel (being confirmed now). Plus a real history-of-science paper (convergent rediscovery; Bidard-Erreygers-Parys "All but one" / Potron 1911).
- #2 Controllability: genuine divergence FVS≠driver-nodes confirmed on OEWN (Jaccard 0.05 on kernel); surviving bridge = FVS-control (Mochizuki/Zañudo), unexploited.
- #3 Ecology: bimodality falsifier triggered; analogy shallow; only diachronic weak form survives, untested.
- #4 Yoneda/Harnad: verdict Resolution A (compatible; grounding = constructing the base category) + symmetry rider (Benacerraf access problem = Harnad's problem); empirical leg blocked on missing norms (ingest agent fixing).
- #5(money): partly real, conflates 4 things, subsumed by PF valuation line; demote to interpretive preface. (this was prompt 1)
- Codex's synthesis: 3-layer architecture (well-foundedness / valuation / dynamics); lead paper = "Discrete Grounding and Spectral Valuation in Dictionary Graphs".

### Psycholinguistic ingest DONE — but in a WORKTREE
Agent a1f0cfc8414c31a55 ran in worktree `.claude/worktrees/agent-a1f0cfc8414c31a55` (branch worktree-agent-a1f0cfc8414c31a55), changes UNCOMMITTED there. Got all 3 datasets: SUBTLEX-US (OSF mirror), Kuperman 2012 AoA (OSF), Brysbaert 2014 concreteness (Springer ESM). Produced loadable CSVs data/psycholinguistic/{frequency,age_of_acquisition,concreteness}.csv (74284/31104/39953 rows, format `word,<field>` matching annotations.py). Verified via CLI: OEWN paper-wordnet coverage now 28.99% freq / 24.11% conc / 21.44% AoA over 160010 nodes (was 0). Also: data/psycholinguistic/README.md, scripts/build_psycholinguistic_csvs.py, rewrote reports/annotation-sources.md, regenerated reports/oewn-paper-wordnet-kernel-{report.md,summary.json}, added pandas+openpyxl to pyproject/uv.lock, notes/psycholinguistic-data.md.
ACTION NEEDED at commit time: bring this worktree's changes into master (commit in worktree + merge, or copy files over). Don't lose it.

### ALL DONE — committing now
Spectral agent done: reverse-PageRank prediction PARTIALLY confirmed (top words yes — large/body/small/water/plant/white all top 0.3%; ρ vs FVS degree-key = 0.316 not >0.6 because reverse-PR ≈ pure out-degree while FVS key sums out+in). Null models: degree alone recovers ρ≈0.68 of the real ranking → neither spectral nor combinatorial object adds much over degree (stated plainly). Added src/meanings/spectral_analysis.py, scripts/spectral_report.py, tests/test_spectral_analysis.py, conftest.py, reports/spectral-valuation-oewn.{md,json}. Repo had NO test suite before; now 7 tests pass (`uv run pytest`). Fixed one brittle test assertion (forward_vs_reverse order-equality → score-delta check) + tidied another.
Reconciled the psycholinguistic worktree into master (copied files over, removed worktree+branch). codex auto-committed 2 extra commits earlier (e15143e codex review, 85cd368 a "workstream-5 minset" report it generated) — harmless, left in place.
Wrote reports/swanson-synthesis.md (the master reconciliation doc — scoreboard, the spectral numbers, controllability divergence, ecology falsifier, yoneda verdict, money demotion, what changed in repo, 6 recommended next steps).
Updated .gitignore: added .claude/ and *.log.
Done — committed in 4 chunks (e6ce622 psycholinguistic data, d096051 spectral module+tests, ecb5147 findings+synthesis, 86b51a3 notes+gitignore), pushed. Working tree was clean.

### Round 2 (2026-05-12 cont.) — bio papers + Q's external deep-research
- Q: "do the bio shit / paper retriever/paper reader on that material" → launched 3 `claude` subagents (background) to retrieve+read the FVS-control biology papers via research-papers:paper-retriever then paper-reader (NO heavy propstore ingestion — repo uses light papers/<name>/notes.md convention): af484a41d34b47ca5 = Fiedler-Mochizuki "Dynamics and control at FVS I" (J Dyn Diff Eq 2013, paywalled Springer), ae3e93dab03e96465 = Mochizuki-Fiedler "...II" (J Theor Biol 2013, paywalled Elsevier), a647a0be4ff625a3c = Zañudo-Yang-Albert "Structure-based control of complex networks with nonlinear dynamics" (PNAS 2017, open access). All 3 told to update papers/index.md + list leads without chasing. Still running.
- Q dropped `C:\Users\Q\Downloads\swanson-money-numeraire-findings.md` = Q's own deep-research thread's output for the money/numéraire link, then said "copy that into the current directory at least under reports". Copied → `reports/swanson-money-numeraire-findings-deepresearch.md`. It's a strong independent twin of our `reports/swanson-money-numeraire-findings.md` — converges hard (same demotion of "MinSet=numéraire", same Sraffa-standard-commodity=PF-eigenvector move, same "Nixon shock line is loose", same proposed title) but adds: an actual publishable THEOREM (c(v)=g(v)/π_v over irreducible weighted Kernel; c-min FVS = "standard MinSet", canonical relative to stated standard not semantically ultimate) and a 4-experiment research agenda (weighted-MinSet frontier / Core fixed-point diagnostics / anchor-swap natural experiments / monetary-graph back-transfer).

Done: provenance header added, synthesis updated with the standard-MinSet theorem + convergence note, committed f356438 + pushed.

### Bio papers status
- DONE: Part II → papers/Mochizuki_2013_DynamicsControlFeedbackVertex/ (PDF via sci-hub.ru, notes.md 27KB). Faithful-monitor/determining-nodes theorem; case studies FVS 5/1/7.
- DONE: Part I → papers/Fiedler_2013_DynamicsControlFeedbackVertex/ (PDF via sci-hub.st, 42pp, notes.md 236 lines). Foundational FVS-control theorem; §8.3 cyclicity-set/Morse decomposition ≈ Rest/Kernel/Core/Satellites; only-if direction proved constructively. Caveat: decay/dissipativity assumption has no clean lexical analogue → for static-fixpoint reading use the combinatorial "informative set" def. Cross-refs wired between Parts I/II/Zañudo. index.md updated.
- STILL RUNNING: a647a0be4ff625a3c (Zañudo-Yang-Albert — agent created papers/Zañudo_2016_Structure-basedControlComplexNetworks/ i.e. the 2016 PLoS Comp Bio / arXiv 1605.08415 version, not PNAS 2017; other 2 agents already cross-ref it so the dir exists on disk).
- NONE committed yet — commit all 3 (or 4) papers/ dirs + index.md + cross-ref edits in one commit once Zañudo agent finishes.

### Round 3 (2026-05-12 cont.): "go go go make all fixes and improvements"
Q is ACTIVELY editing the repo in parallel — MinSet solver work (commits bfe7ae8/faa8a6d/f1543e6/c8b98dc/ee2a43b/e5724d8 + uncommitted cli.py change adding `exact-cutting` seed method). My commits interleave cleanly (different files). Be careful NOT to touch graph_analysis.py / cli.py / anything seed/MinSet-solver-related.

DECISION on compute_kernel self-loop "bug": NOT fixing it. Line 88 excludes self-loops (`target != node`) so a word appearing only in its own gloss gets peeled out of the Kernel. Whether self-loops belong in the Kernel is a SEMANTIC MODELING decision (1-node self-loop = forced FVS member by std defn, but the dict-graph papers may deliberately exclude gloss self-loops as parsing artifacts) — I haven't verified what Massé/V-L do, and Q is editing this exact area. Flagged to Q, left alone.

DONE this round so far:
- Committed bio papers (6a931a8, pushed).
- Cleaned up src/meanings/spectral_analysis.py: added SCCEigenvector dataclass (was returning list[dict[str,object]] → pyright noise + clunky indexing), updated call site + scripts/spectral_report.py + tests/test_spectral_analysis.py to use attrs not dict-keys, added to __all__, removed unused `deque` import, rewrote the confused half-corrected orientation docstring. `uv run pytest` → 10 passed.
- LAUNCHED 4 background agents: af242e58f214a5470 = psycholinguistic regressions (Yoneda/PF empirical legs → reports/psycholinguistic-regression-findings.md + scripts/psycholinguistic_regression.py); a78d96229295735d3 = paper Liu-Slotine-Barabási 2011 Nature "Controllability of complex networks"; af122daf195d41291 = paper Mochizuki-Saito 2010 "informative nodes" (J Theor Biol); a46ad51649a269e51 = paper Gates-Rocha 2016 Sci Rep "Control...requires both structure and dynamics". All told to stay out of src/.

### Round-3 agent status
- DONE Gates-Rocha 2016 → papers/Gates_2016_ControlComplexNetworksRequires/ (nature.com direct). Boolean-ensemble result: SC + MDS both over/under-estimate driver set & pick wrong vars once dynamics present; canalization is the lever. NOT committed.
- DONE Mochizuki-Saito 2010 → papers/Mochizuki_2010_AnalyzingSteadyStatesDynamics/ (DOI 10.1016/j.jtbi.2010.06.007 — note second author is Daisuke Saito not "Bernold", my prompt slipped; sci-hub.ru). "Linkage logic" / informative nodes = steady-state precursor to FVS-control. Cross-refs to Fiedler_2013/Mochizuki_2013. NOT committed.
- STILL RUNNING: a78d96229295735d3 (Liu-Slotine-Barabási 2011 Nature — papers/Liu_2011_ControllabilityComplexNetworks/ dir exists, PDF+pngs present, notes.md pending), af242e58f214a5470 (psycholinguistic regressions).
- Working tree also has a papers/Cho_2011_* dir I did NOT create — Q's or Q's deep-research thread's.
- spectral_analysis cleanup committed 194a84a + pushed.

### Regression agent DONE
af242e58f214a5470 done: psycholinguistic block adds ΔR²/ΔAUC 0.0001–0.010 over structural features for every layer/membership outcome (< 0.05 threshold) → strong-residue claim refuted, Yoneda-completeness untouched (lexicographers-write-for-learners confound is causal-direction-fatal). revPR/FVS-seed add ≤+0.03 R² over log(out-degree) → Codex's "degree explains most" confirmed; revPR = out-degree spectrally laundered; fwd PageRank ~zero-corr with norms. Vincent-Lamarre "deeper=later AoA" effect VANISHES under structural controls. Files: reports/psycholinguistic-regression-findings.md, reports/psycholinguistic-regression-output.json, scripts/psycholinguistic_regression.py; added scikit-learn+statsmodels to pyproject/uv.lock; pytest 10/10. NOT committed.
Folded these into reports/swanson-synthesis.md (Yoneda section, PF null-model caveat, recommended-next-work #2/#3, "what changed" section incl. listing all the new papers). NOT committed.

### State of working tree (uncommitted, mine to commit)
M: notes-swanson-prompts.md, reports/swanson-synthesis.md, pyproject.toml, uv.lock, papers/{Fiedler_2013,Mochizuki_2013,Zañudo_2016}/notes.md+citations.md, papers/index.md (all cross-ref edits from the bio + new-paper agents)
?? mine: papers/{Gates_2016_ControlComplexNetworksRequires,Mochizuki_2010_AnalyzingSteadyStatesDynamics,Liu_2011_ControllabilityComplexNetworks}/, reports/psycholinguistic-regression-{findings.md,output.json}, scripts/psycholinguistic_regression.py, notes/liu-2011-retrieval.md
?? NOT mine (Q's or stray, do NOT commit): papers/Cho_2011_AbelianDecompositionGeneralRelativity/ (unrelated GR paper, only metadata+pdf, looks abandoned)
Liu_2011 dir has notes.md+abstract.md but no description.md/citations.md yet → agent a78d96229295735d3 still mid-write, NOT reported done. WAIT for it before committing papers/.

### Round 3 COMPLETE — all committed + pushed
Commits 0a4d9b1 (network-control lineage papers + cross-refs), 69882d2 (psycholinguistic regressions + deps + script), 29d941a (synthesis update + notes). Liu agent also cleaned up its own stray Cho dir. Q's parallel MinSet-solver commits (c83746e etc.) pulled in cleanly — linear history, no conflicts throughout.

Paper collection now has the full FVS-control ↔ structural-controllability lineage: Liu_2011 (matching baseline), Mochizuki_2010 (informative nodes precursor), Fiedler_2013 + Mochizuki_2013 (FVS-control I & II), Zañudo_2016 (FVS+sources, empirical comparison), Gates_2016 (structure-only mis-identifies). Plus the 5 swanson findings + deepresearch twin + synthesis + codex review + psycholinguistic-regression findings, spectral_analysis.py module + tests, psycholinguistic data, scratch scripts.

OPEN ITEMS (none in progress):
- compute_kernel self-loop handling — DONE (commit 7d12e64). Fixed FVS-consistent: a self-loop is a cycle, node stays in Kernel. tests/test_graph_analysis.py added (15 tests pass).
  CHECKED DOWNSTREAM (Q told me to; I wrote scripts/self_loop_fix_impact.py instead of ad-hoc python -c after Q called me out for that):
  - OEWN paper-wordnet has **3,413 self-loops** (real — glosses repeat headword: violin::n, read::n, found::v, ...). NOT artifacts, NOT a no-op fix.
  - Numbers shift a lot. bounded-scc: kernel 12,853→18,151, core 288→510, satellites 12,565→17,641, seed 2,370→3,620, kernel_scc_count 3,841→9,139. So committed reports/oewn-paper-wordnet-kernel-{report,summary} are now stale (Q's MinSet pipeline regens them).
  - solve_minset DOES handle self-loops (all 3 methods include self-looped singletons in the seed; 2,965/3,413 self-loops in the bounded-scc seed). So consequence #2 (self-loop node un-FVS'd → layer skip) does NOT happen.
  - BUT: with the bigger post-fix kernel, residual_cyclic_scc_count goes 0→1 for bounded-scc (8-pass cap) and 0→11(?) for exact-cutting (single-pass cutter) → analyze_kernel skips compute_layer_map → **layer histogram comes out empty** for those two methods. Only `exact-small-greedy` (loops to completion: seed 5,044, residual 0) still produces layers. Script bp0de20nn running to get the pre-fix baseline column to confirm whether exact-cutting's residual was already nonzero pre-fix.
  - PRE-FIX BASELINE (from scripts/self_loop_fix_impact.py, self-loop-free graph + current code): bounded-scc pre-fix already had residual_cyclic_scc=1, NO layers, seed 733 (NOT the 2,370 in the committed summary — committed reports predate the MinSet-solver refactor too). exact-cutting pre-fix residual 1 → post-fix 11. exact-small-greedy: residual 0, layers yes, both (seed 1,824→5,044). So: my fix's ONLY effect = kernel size 12,853→18,151; the no-layers situation for bounded-scc predates the fix (it's the 8-pass cap on a big kernel, from Q's refactor); the fix worsens exact-cutting (1→11 residual). exact-small-greedy is the robust one.
  - Wrote scripts/self_loop_fix_impact.py + reports/self-loop-fix-impact.{md,json} (regenerating with corrected note now, bg bd06i0uv7). Q corrected me for using `python -c` one-liners instead of scripts — acknowledged, won't repeat. Need to commit script+report once regen done.
  - REMAINING for Q's call (NOT my code): bump bounded-scc max_passes, change default seed method, or accept exact-small-greedy is the layer-producing one. Also: update swanson-synthesis.md / spectral findings which quote the now-stale 12,853/2,370/~1.5% numbers.

### NEW DIRECTIVE (2026-05-12): sibling tools
Q: "consider ../argumentation, ../belief-set, ../gunray ... think really hard ... figure out if these tools can be of any use here? Draw the connection."
- ../argumentation = pure-Python formal argumentation lib (Dung AAFs, ASPIC+, ABA/ABA+, ADFs, bipolar/value-based AFs, SETAFs, partial AFs, AF revision/enforcement/dynamics, probabilistic + gradual/ranking semantics, SAT/ASP/datalog encodings, ICCMA). ~21K lines per old memory note.
- ../belief-set = finite propositional belief-revision (AGM revision/contraction incl. "kernel contraction", Levi/Harper, epistemic entrenchment, Spohn ranking/OCFs, iterated revision DP/Jeffrey/Boutilier, IC merging). 2^n worlds.
- ../gunray = defeasible logic engine (Nute; named for Donald Nute via Nute Gunray). DefeasibleTheory(facts/strict/defeasible/defeaters), GunrayEvaluator, ClosurePolicy/MarkingPolicy (BLOCKING etc.), UNDECIDED as first-class, superiority relations, "why" traces. MIT, zero deps.
- LAUNCHED 3 scout agents → reports/sibling-tool-{argumentation,belief-set,gunray}.md: abeca878a4c43b668 (argumentation), a21f1cbac3eba4045 (belief-set), ad1ea05b40489fac5 (gunray).
- MY DRAFT CONNECTION (to synthesize against their findings, write reports/sibling-tools-connection.md):
  (1) Definitions = ADF acceptance conditions (you know w iff you know [combo of] its definiens); definition digraph = bipolar AF (definition edges = support, polysemy/exceptions = attack). Probably the most natural formalization of a dictionary graph ever written down — and we own the ADF impl.
  (2) FVS / MinSet ↔ AF enforcement / revision: removing an FVS makes the AF acyclic ⇒ unique extension; the FVS is "minimal set of arguments whose status you must fix externally to make the framework's verdict determinate" = exactly "minimal vocab you must ground externally so every other word's meaning is determined by unfolding". MinSet non-uniqueness ↔ multiple stable extensions (even cycle → 2 stable extensions ↔ 2-cycle definition A↔B → MinSet {A} or {B}). Grounded (skeptical, unique) extension ↔ the "Rest" (the acyclic everyone-agrees part).
  (3) Kernel (foundationalism) vs loop-ecology (coherentism) = different argumentation semantics over the same graph (grounded vs preferred/stable); argumentation theory gives the relationships for free. This is the Harnad-vs-distributionalist debate, formalized.
  (4) Dictionary editions = AGM iterated revision; epistemic entrenchment ↔ "how foundational/resistant-to-redefinition" ↔ Kernel/Core; Spohn rank / OCF = a graded foundationalness measure (cf. the layer index = already an integer ranking; cf. spectral valuation trying & failing to be graded). belief-set's "kernel contraction" even shares the WORD "kernel" with the meanings repo's Kernel — probably coincidence but worth checking the formal relation (AGM kernel = minimal subsets implying the retracted belief; meanings Kernel = recursively irreducible cyclic part).
  (5) Polysemy / sense disambiguation = defeasible reasoning (gunray / ASPIC+): sense_i and sense_j attack; context = arguments; accepted extension = disambiguated reading. UNDECIDED = the right answer for a genuinely circular ungrounded definition.
  (6) Gradual/ranking-based argumentation semantics (h-categorizer etc.) = the spectral-valuation line done right (principled "how defensible/grounded" vs PageRank's pure-degree).
  ONE-SENTENCE: the meanings repo does a thin graph-theory-only version of "what does a word mean when the supporting structure is circular and underdetermined"; argumentation/belief-revision/defeasible-logic have rich citation-anchored machinery for exactly that, and Q owns reference implementations of all of it.

### Next step
Commit self_loop_fix_impact.py + report (after bd06i0uv7 finishes). Wait for the 3 sibling-tool scouts. Synthesize reports/sibling-tools-connection.md from my draft + their findings. Present to Q.

### Round 4 (2026-05-12 cont.): sibling-tools connection + scaling
- self_loop_fix_impact.py + report committed 9da2f92. Q accepted the self-loop fix, built on it (Up-Goer workstream, commits e01b3da/2546621).
- 3 sibling-tool scouts done → reports/sibling-tool-{argumentation,belief-set,gunray}.md. Synthesized reports/sibling-tools-connection.md (the 6-point connection + per-connection feasibility verdicts + the three-distinct-"kernel"s footnote). Committed 88c46a1, pushed.
- Q pushed back on my "demonstrator-tier, won't scale" framing: "can argumentation just git gud and handle 160k? is it impossible? we're already using z3 — more magic graph shit?" My answer (given in full to Q): NOT impossible. Grounded semantics is polynomial → trivial at 160k. The hard semantics (stable NP / preferred Π₂ᵖ) are tractable on THIS graph's structure — mostly-acyclic, small SCC-decomposable Kernel (~9k SCCs, most tiny), small per-SCC FVS = the FPT backdoor parameter (Dvořák-Pichler-Woltran); z3/SAT handles residual SCCs (160k-var SAT is small). The "magic graph shit" = SCC condensation + FVS-backdoor branching + treewidth/tree-decomp DP + DAG fast-path + backbone propagation + AF splitting + dynamic/incremental. Only truly impossible thing: enumerating exponentially-many objects in poly time — and you never need that (need existence / canonical answer / per-SCC structural count). The thing to BUILD = a divide-and-conquer front end wiring meanings's graph toolbox into ../argumentation's solver layer. Genuine caveat: minset solver's residual cyclic SCC means there's ≥1 big/hard SCC — z3 carries it, probably fine, proof is in running it.
- Q said yes → LAUNCHED agent a98e413645ab85638 (claude): build src/meanings/argumentation_bridge.py + scripts/argumentation_bridge_oewn.py + reports/argumentation-bridge-oewn.md + tests; uv add formal-argumentation (git or local editable); run GROUNDED semantics on full 160k graph (attack-reading + bipolar/support-reading), report alignment with the leaf-stripping Kernel decomposition + timing; probe stable-extension feasibility via z3 SAT encoding on the Kernel subgraph / per-SCC; verdict on whether full-scale argumentation is feasible-today / feasible-with-divide-and-conquer / blocked-on-which-SCC. Told to NOT touch minset.py/graph_analysis.py/cli.py/workstreams/. Still running.
- Q also said "take some more notes" → wrote 3 project-memory files in ~/.claude/projects/C--Users-Q-code-meanings/memory/ (MEMORY.md index + dictionary-as-argumentation-framework.md + sibling-tools-argumentation-belief-gunray.md + meanings-repo-conventions.md) + this update.

### Round 5 (2026-05-12 cont.): perf-fixes to ../argumentation + ingestion-layer + synthesis spec
- argumentation-bridge agent done; committed ab7d13d. Grounded at 160k = 0.8s via labelling (lib's own grounded_extension is super-quadratic, dies); grounded(attack) ≠ leaf-stripping Kernel (it's an IN/OUT/UNDEC labelling of the acyclic shell + 468 forced Kernel nodes; grounded∪Rest = nodes−Kernel-UNDEC; seed overlap 2); Kernel AF has NO stable extension (whole Kernel UNSAT in 7.4s via z3, 8138-node core SCC UNSAT in 3.3s, 63/693 SCCs UNSAT); nothing blocked on a hard SCC. Path forward = SCC+z3+FVS-backdoor divide-and-conquer front end + upstream the labelling fix into ../argumentation.
- Q asked: do we precisely understand the ../argumentation fixes? YES — Fix A: dung.grounded_extension fixpoint-over-characteristic-fn → standard O(V+E) labelling (IN if all attackers OUT etc.), output identical. Fix B: bipolar.bipolar_grounded_extension recomputes _defeat_closure + attackers index PER CALL → compute closure once, delegate to fixed dung.grounded; add optional precomputed-closure kwargs to bipolar.defends/characteristic_fn. Both verifiable bit-exact. Out of scope: SCC dispatcher (a feature, build in meanings first).
- Q said: ask Codex to implement, verify with subagent. → Wrote ../argumentation/prompts/perf-fixes-grounded.md (precise spec + acceptance bar). LAUNCHED `codex exec` in ../argumentation (bg bshhje9er, log codex-perf-fixes.log) → it edits dung.py + bipolar.py, adds equivalence+scaling tests, writes ../argumentation/reports/perf-fixes-grounded-report.md, no commit. STILL RUNNING. WHEN DONE: dispatch a verifier subagent (independent equivalence battery + full ../argumentation suite + scaling test via meanings/scripts/argumentation_bridge_oewn.py against patched lib + scope check → MERGE/NO-MERGE).
- Q shared notes/upgoer-identity-clusters.md (form/sense/IC/reading/admission, referential-vs-indexical, "embeddings are evidence not authority", typed/inspectable/falsifiable). Asked A (fold pipeline-as-defeasible-inference + self-loop-artifact prediction into connection report + memory) and B (sketch typed sense/IC node model as ADF over SENSE nodes not lemma::pos). DONE: added §7 "The ingestion layer..." to reports/sibling-tools-connection.md (covers A + B: pipeline maps onto enforcement[reading]/defeasible-WSD[sense]/belief-merge[IC]/referential-vs-indexical-separation/defeasible-admission-theory; ADF over sense nodes is the honest granularity; falsifiable prediction: sense-level Kernel shrinks vs the artifact-inflated lemma-level one). Added next-step #7 (sense-level ingestion rebuild = the project's biggest open redesign, successor to Q's current Up-Goer export).
- Q: do I have claude -p as a subagent mechanism? Checked: my Agent tool spawns claude-type subagents (= claude -p under the hood, managed). Read ~/.codex/agents.md → the explicit CLI pattern: `claude -p --effort high --dangerously-skip-permissions --permission-mode bypassPermissions "..."` via Bash, poll up to 10min, then one adversarial follow-up. Useful for putting a sub-Claude on the SAME footing as Codex/Gemini as a peer critic.
- Q: "let's start speccing that out" (multi-contributor synthesis). DONE: wrote reports/synthesis-workflow-spec.md — goal (integrate kernel work + Swanson links + argumentation reframing + sibling tools + scaling + ingestion redesign into one synthesis answering 5 questions); contributors (orchestrator=me, 4 facet sub-Claudes [math/philosophy/datamodel/engineering], scout, analyst, codex+gemini+claude-p as 3 peer critics with non-overlapping attack angles, verifier gate); 6-phase pipeline (inventory → facet deep-dives → integration draft → adversarial multi-review → revision → verification gate); filesystem artifacts in reports/; 4 open questions for Q at the bottom.

### Perf-fix DONE + MERGED
- Codex implemented (../argumentation/reports/perf-fixes-grounded-report.md): dung.grounded_extension → O(V+E) labelling (new _targets_index helper); bipolar.bipolar_grounded_extension → compute Cayrol closure once + delegate; additive precomputed-closure kwargs on bipolar.defends/characteristic_fn. 803→806 passed. Microbench: old times out >2s at 4800 nodes; new does 50k in 0.14s.
- Verifier (a2593a3cdc66ad9ad) → MERGE. Scope clean; suite 806 passed; 257 Dung + 120 bipolar instances all equal to independent reference fixpoint; scaling: 50k→0.13s (binding bar), real 160k OEWN graph ~1.5s (median, |grounded|=5043 matching bridge agent — the ~1.5s is the module's dict[str,frozenset] index convention overhead, not a regression, vs >200s-killed before). One note: ~1.5s isn't "well under a second" — Q may or may not care; flagged in reports/verify-argumentation-perf-fixes.md.
- Committed ../argumentation @ 4a1c31cc45a2a4a67b45a72941812e1098ba7018 (src/argumentation/{dung,bipolar}.py + tests/test_grounded_perf_equivalence.py + prompts/perf-fixes-grounded.md + reports/perf-fixes-grounded-report.md only — NOT the pile of unrelated untracked notes/reports there), pushed. Then updated meanings/pyproject.toml pin 8d28624→4a1c31cc, uv lock, verified `import argumentation; grounded_extension` source is the labelling version, pytest 24 passed. Committed meanings 26d2dd7 (pyproject+uv.lock+.gitignore[scratch/]+verify report), pushed. Verification scratch scripts left untracked under scratch/ in both repos (now gitignored in meanings).

### Round 5 cont.: Q flagged Claude-favoritism in the synthesis spec
Q: "you distinctly don't trust the gemini/codex agents ... claude gets to do everything interesting." Fair — the spec put all 4 facets on sub-Claudes, Codex/Gemini as critics only, Gemini did nothing real yet, and "Claude verifies everyone" (Claude verifier on Codex's perf fix). Rebalanced reports/synthesis-workflow-spec.md (commit pushed): only orchestrator is necessarily Claude (context); facets distributed (math→Codex incl. running the scaling check; engineering→Claude; data-model→Claude+Gemini twin drafts; philosophy→Gemini+Codex twin drafts, deliberately NOT Claude — the facet Claude's most likely to be eloquently wrong on); 2 highest-stakes facets written twice by different families, divergence = a finding; critics don't self-review co-authored facets; verifier rotates by load; verification invariant = "different agent, ideally different model". Updated the open-questions list (now 5: scope / facet cut incl. whether to split Swanson into a 5th / is the rebalance distribution right / who assigns critic angles / launch 0-1 first vs all). STILL HOLDING for Q's answers.

### ROUND 6 (2026-05-12): THE SWARM — "do everything at once, fuck Brooks, we have agents"
Q: "Go, ask codex to start the actual work ... the stuff we have actually said ... and then check it ... we can do everything at once". Launched 12 things in parallel. Q also said: MERGE the sense-rebuild worktree when done, don't let it drift, clean up after.

Created worktree: C:/Users/Q/code/meanings-sense-rebuild on branch `sense-rebuild` (from HEAD 6caa149). MUST: when the sense-rebuild codex is done → review → merge `sense-rebuild` into master (or cherry-pick) → `git worktree remove ../meanings-sense-rebuild` → `git branch -d sense-rebuild` (or -D). DO NOT LEAVE IT DANGLING.

AGENT ROSTER (round 6):
Synthesis phases 0-1 (report-only, no src risk):
- a8252a72ebae23d84 = scout → reports/synthesis-inventory.md  [WAIT: this ID may be wrong — 7 agents dispatched in one block, IDs in order: ab80e56c3195613a7, aadd761c0aa5bdaa5, a0c38e3793755066e, a9bdc2d5fa6dd1900, a8eb78a785ab6ed33, ac1699d0851c9280d, a8252a72ebae23d84 = scout, eng-facet, datamodel-facet-claude, ranking-valuation, gunray-demo, stale-numbers, scc-dispatcher respectively]
- ab80e56c3195613a7 = scout → reports/synthesis-inventory.md
- aadd761c0aa5bdaa5 = engineering facet → reports/synthesis-facet-engineering.md
- a0c38e3793755066e = datamodel facet (Claude draft) → reports/synthesis-facet-datamodel-claude.md
- bnznklog8 (codex bg) = math/complexity facet → reports/synthesis-facet-math.md (log codex-facet-math.log)
- b5nto9th5 (codex bg) = philosophy facet (codex draft) → reports/synthesis-facet-philosophy-codex.md (log codex-facet-philosophy.log)
- b13t40rbg (gemini bg) = datamodel facet (Gemini draft) → reports/synthesis-facet-datamodel-gemini.md (log gemini-facet-datamodel.log) [verify gemini didn't just copy]
- bsw1isdti (gemini bg) = philosophy facet (Gemini draft) → reports/synthesis-facet-philosophy-gemini.md (log gemini-facet-philosophy.log) [verify]
Implementation (the "actual defined work"):
- a9bdc2d5fa6dd1900 = ranking-semantics valuation (#6) → scripts/ranking_valuation_oewn.py + reports/ranking-valuation-oewn.{md,json}. Owns those; may add a fn to argumentation_bridge.py; NOT minset/graph_analysis/cli/wordnet_pipeline/pyproject/workstreams.
- a8eb78a785ab6ed33 = gunray UNDECIDED demonstrator (#5) → adds `gunray` dep (the ONLY agent touching pyproject this round) + scripts/gunray_grounding_demo.py + reports/gunray-grounding-demo.md.
- ac1699d0851c9280d = update stale Kernel numbers → edits reports/swanson-synthesis.md, spectral-valuation-oewn.md, swanson-perron-frobenius-findings.md, sibling-tools-connection.md; regenerates stale JSONs; adds reports/stale-numbers-audit.md. Reports only, no src.
- a8252a72ebae23d84 = SCC+z3+FVS-backdoor dispatcher → NEW module src/meanings/argumentation_dispatch.py + scripts/argumentation_dispatch_oewn.py + reports/argumentation-dispatch-oewn.{md,json} + tests/test_argumentation_dispatch.py. NOT touching argumentation_bridge.py/graph_analysis/minset/cli/wordnet_pipeline/pyproject/workstreams.
- b9shl8smj (codex bg, IN THE WORKTREE) = sense-level ingestion rebuild per notes/upgoer-identity-clusters.md "Next Executable Workstream" (writes notes/sense-ingestion-design.md, src/meanings/lexicality.py, src/meanings/identity_clusters.py, a sense-level graph builder, two export surfaces, reports/sense-ingestion-rebuild.md; commits freely on the branch). (log codex-sense-rebuild.log)

COLLISION MANAGEMENT: report-only agents write distinct files in reports/ — fine. Only the gunray agent touches pyproject. Only the dispatcher agent adds src/meanings/argumentation_dispatch.py (new file). The sense-rebuild is isolated in the worktree. Q is also editing master (Up-Goer workstream, minset.py). When all done: reconcile — commit the report-only stuff, the new src modules + tests, the dep changes; merge the worktree branch; pull-rebase Q's commits throughout.

### Round 6 progress (landings)
COMMITTED so far: math facet (fb606f0), gunray demo (d249923), dispatcher (c50bf7f), stale-numbers refresh (0047818), then the sense-rebuild reconciliation:
- The sense-rebuild codex ran in worktree ../meanings-sense-rebuild (branch sense-rebuild, 7 atomic commits). Branch was based on OLD master (6caa149) — so `git diff master..sense-rebuild` showed a pile of fake "deletions" (files on current master not on the branch base). The 7 commits' NET effect (diff from 6caa149) was PURELY ADDITIVE: 11 new files, no modifications to existing files (it ADDED build_sense_level_paper_wordnet_graph to wordnet_pipeline.py, didn't touch the existing fn). So: cherry-picked the 6 code/notes commits (46c36b2..41a8c30) onto master cleanly → became 066af23/e2466c1/etc.; for the 7th (export artifacts) I copied the 3 small files by hand (reports/sense-ingestion-rebuild.md, reports/oewn-sense-ingestion-summary.json, data/oewn-sense-strict-seed.json 358KB) + GITIGNORED data/oewn-upgoer-sense-vocabulary.json (35 MB, 1.59M lines — too big for git history; regenerable via scripts/sense_ingestion_rebuild.py) → commit 2314ab9. Removed the worktree + deleted the branch. 60 tests pass.
- SENSE-REBUILD RESULT: sense-level graph = 212,478 sense nodes / 418,094 edges; Kernel 12,142 (vs lemma-level 18,151), Core 1,939, Sat 10,203, seed 1,582, residual cyclic SCCs after seed = 0 (acyclic closure holds). ZERO surviving gloss self-loops (vs 3,413) — the self-loop-artifact prediction PASSES. 4,562 short non-lexical artifacts (no::n=Nobelium etc.) quarantined; 7 spelling-variant ICs over 14 forms merged. New modules: src/meanings/lexicality.py (lexicality classifier, 9-tag enum), src/meanings/identity_clusters.py (spelling-variant IC merge records), wordnet_pipeline.py +build_sense_level_paper_wordnet_graph, scripts/sense_ingestion_rebuild.py, notes/sense-ingestion-design.md (the type contract). Exports: data/oewn-sense-strict-seed.json (834 lexical-IC seed), data/oewn-upgoer-sense-vocabulary.json (121,375 admitted ICs, gitignored). CAVEAT: Kernel-shrinkage isn't apples-to-apples (different node surface — every OEWN sense kept vs collapsed to lemma::pos); the load-bearing claim is self-loop dissolution + artifact removal, not the raw size drop.

STILL RUNNING: b5nto9th5 (codex, philosophy facet → reports/synthesis-facet-philosophy-codex.md) [LAST facet draft]; a9bdc2d5fa6dd1900 (claude, ranking valuation — mid-run, has written scripts/ranking_valuation_oewn.py + notes/ranking-valuation.md, NOT yet reports/ranking-valuation-oewn.md; it's in an auto-worktree .claude/worktrees/agent-a9bdc2d5fa6dd1900 — handle when done).
UNCOMMITTED in main tree (to commit when their agents finish): scripts/ranking_valuation_oewn.py, notes/ranking-valuation.md, notes/stale-numbers-audit.md.

### Round 6 — phases 2+3 done/launched
- All 6 facet drafts committed (engineering fb606f0-area, math fb606f0, datamodel-claude+gemini 85a5294-area, philosophy-gemini 85a5294, philosophy-codex dce01a4 [re-launched b2lb390au with `< /dev/null` after the original b5nto9th5 hung on stdin — Q: "you launched codex correctly before -- do it right" → henceforth use `< /dev/null` on codex/claude-p launches; renamed -codex2.md → -codex.md]).
- PHASE 2 DONE: wrote reports/synthesis.md (integration draft), committed 15b7411. Five-question structure; reconciles the twin pairs (datamodel: no conflict, one open IC-projection question; philosophy: convergent — both model families same conclusions); includes §6 research agenda + §7 "open for a critic" + §8 twin reconciliation. (Folded the agenda/outline into synthesis.md rather than 3 separate files — the spec's a draft anyway.) NOTE: didn't write separate reports/synthesis-research-agenda.md / synthesis-paper-outline.md — they're §6 of synthesis.md; could split out later if Q wants.
- PHASE 3 LAUNCHED (4 things, bg): codex bqd17nmbn → reports/synthesis-review-codex.md, angle = "the sense-level rebuild's numbers are cooked / lexicality classifier precision unaudited / IC-merge whitelist is 7 hand-picked pairs not a method / Kernel-shrinkage not apples-to-apples / is the 121k 'admitted extension' just a lexicality filter dressed up". gemini bqmrb4qqj → reports/synthesis-review-gemini.md, angle = "the argumentation reframing is a relabeling not a result — is MinSet=enforcement a theorem or a translation dictionary; what does ADF/bipolar buy that compute_kernel didn't; the stable-extension story collapsed (Kernel AF is UNSAT) so what survives; is 'argumentation can't scale is retired' an over-claim when only grounded (poly) + one z3 call scaled". claude-p bqvdxl6cf → reports/synthesis-review-claude.md, angle = "reinventing distributional semantics with extra typed steps and worse data — the spectral/regression/ranking results ALL say 'structure mirrors degree/norms' = the distributional thesis, so the project's own evidence argues FOR distributional semantics; does the typed system beat an embedding at anything; is WordNet the right data or a sunk cost; the lexicographer's confound means the graph measures lexicographers' folk-models of learners". analyst ad2cf1b065dd4b58f → reports/synthesis-analyst-findings.md (contradictions / uncited claims / stale numbers / load-bearing-but-unverified / over-claims / internal inconsistency, numbered with severity + minimal fix).

### Next step (round 6 cont.)
1. Wait for the 3 reviews + analyst. Read them. PHASE 4: revise reports/synthesis.md — every critique addressed in-text OR explicit "we disagree because" OR "noted, follow-up task X"; nothing silently dropped.
2. PHASE 5: rotating verifier gate (claude -p or codex exec — whichever authored least) on the revised synthesis vs the spec's acceptance bar → SHIP/NO-SHIP.
3. PHASE 6: present to Q.
4. Still owed: a verifier pass on the new src (lexicality.py / identity_clusters.py / the sense builder / argumentation_dispatch.py — tests pass but no independent audit). And: the bipolar-AF/ADF demonstrator over the SENSE-level graph + the sign-correct ranking re-run (agenda #3 — the thing the argumentation reframing has promised and not shown). The paper(s). Cross-dictionary confound break (blocked on LDOCE data). The IC-projection design decision (#5). The IC-merge method (#6). int-indexed argumentation perf (#8). Cross-language (#9).
Still pending after the swarm: writing the paper(s); cross-dictionary confound break; further ../argumentation perf (int-indexed adjacency for sub-second-at-160k grounded).
- Cross-dictionary comparison (vs Longman controlled defining vocabulary) — the way to break the lexicographers-write-for-learners confound on the Yoneda regression.
- Unchased leads: Cowan 2012, Sun-Motter 2013, Marques-Pita-Rocha 2013, Ruths-Ruths 2014, Lin 1974, Liu-Barabási 2016 RMP survey, Cowan/Marchiori/etc.
- Line-ending churn from a linter hook keeps re-touching papers/*.md — a .gitattributes (eol=lf) would stop it but risks a big renormalization diff; left alone.
- The "Discrete Grounding and Spectral Valuation in Dictionary Graphs" paper itself isn't written (synthesis lists it as recommended-next-work #1).

### Note for future agents
The "research-papers" plugin skills are available here; `papers/` follows that convention (`notes.md`/`abstract.md`/`description.md`/`citations.md` per paper, `papers/index.md`). PDFs and PNGs are gitignored.
